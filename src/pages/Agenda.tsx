import { Calendar } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const Agenda = () => (
  <PlaceholderPage title="Agenda" subtitle="Visualize, crie e gerencie eventos do Google Calendar e Outlook com agendamento inteligente." icon={Calendar} />
);

export default Agenda;
