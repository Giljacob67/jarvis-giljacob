import { Send } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const Telegram = () => (
  <PlaceholderPage title="Telegram" subtitle="Envie e receba mensagens do Telegram diretamente pelo Jarvis." icon={Send} />
);

export default Telegram;
