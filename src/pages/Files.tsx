import { FolderOpen } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const Files = () => (
  <PlaceholderPage title="Arquivos" subtitle="Acesse e busque arquivos no Google Drive e OneDrive diretamente pelo Jarvis." icon={FolderOpen} />
);

export default Files;
