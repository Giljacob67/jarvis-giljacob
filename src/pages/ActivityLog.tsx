import { ClipboardList } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const ActivityLog = () => (
  <PlaceholderPage title="Log de Atividades" subtitle="Histórico completo de tudo que o Jarvis executou: e-mails, eventos, mensagens e tarefas." icon={ClipboardList} />
);

export default ActivityLog;
