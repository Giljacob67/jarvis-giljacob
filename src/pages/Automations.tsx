import { Zap } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const Automations = () => (
  <PlaceholderPage title="Automações" subtitle="Gerencie webhooks do Make, regras e fluxos de automação ativos." icon={Zap} />
);

export default Automations;
