import { StickyNote } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const NotionPage = () => (
  <PlaceholderPage title="Notion" subtitle="Crie tarefas, anotações e projetos no Notion por voz ou chat." icon={StickyNote} />
);

export default NotionPage;
