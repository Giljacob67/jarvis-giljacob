import { Settings as SettingsIcon } from "lucide-react";
import PlaceholderPage from "@/components/PlaceholderPage";

const SettingsPage = () => (
  <PlaceholderPage title="Configurações" subtitle="Conecte contas OAuth, configure preferências de voz, idioma e perfis." icon={SettingsIcon} />
);

export default SettingsPage;
